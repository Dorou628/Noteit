<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.noteit.file.repository.UploadTaskMapper">

    <resultMap id="UploadTaskResultMap" type="com.example.noteit.file.model.UploadTaskDO">
        <constructor>
            <idArg column="id" javaType="_long"/>
            <arg column="user_id" javaType="_long"/>
            <arg column="biz_type" javaType="string"/>
            <arg column="file_name" javaType="string"/>
            <arg column="content_type" javaType="string"/>
            <arg column="content_length" javaType="_long"/>
            <arg column="object_key" javaType="string"/>
            <arg column="public_url" javaType="string"/>
            <arg column="status" javaType="_int"/>
            <arg column="etag" javaType="string"/>
            <arg column="expired_at" javaType="java.time.LocalDateTime"/>
            <arg column="created_at" javaType="java.time.LocalDateTime"/>
            <arg column="updated_at" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <insert id="insert">
        INSERT INTO upload_task (
            id, user_id, biz_type, file_name, content_type, content_length,
            object_key, public_url, status, etag, expired_at
        ) VALUES (
            #{id}, #{userId}, #{bizType}, #{fileName}, #{contentType}, #{contentLength},
            #{objectKey}, #{publicUrl}, #{status}, #{etag}, #{expiredAt}
        )
    </insert>

    <select id="findById" resultMap="UploadTaskResultMap">
        SELECT id, user_id, biz_type, file_name, content_type, content_length,
               object_key, public_url, status, etag, expired_at, created_at, updated_at
        FROM upload_task
        WHERE id = #{id}
    </select>

    <update id="markConfirmed">
        UPDATE upload_task
        SET status = 2,
            etag = #{etag},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND user_id = #{userId}
    </update>
</mapper>
